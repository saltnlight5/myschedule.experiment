# Define a standard output appender
log4j.appender.CONSOLE=org.apache.log4j.ConsoleAppender
log4j.appender.CONSOLE.layout=org.apache.log4j.PatternLayout
log4j.appender.CONSOLE.layout.conversionPattern=%d{HH:mm:ss} %t %-5p %c:%L | %m%n

# Define the root logger/global logging level with all appenders, default to use INFO level.
log4j.rootLogger = INFO, CONSOLE

# TimeMachine scheduler application
log4j.logger.timemachine = DEBUG

# We want these loggers to be above INFO so we do not get clutter up the maven
# build result reading. User may turn it back on if needed for troubleshooting 
#log4j.logger.org.springframework = WARN
#log4j.logger.org.hibernate = WARN

# Hibernate logging level to show binding parameters.
#log4j.logger.org.hibernate.SQL = DEBUG
#log4j.logger.org.hibernate.type = TRACE
